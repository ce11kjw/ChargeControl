from flask import Flask, request, jsonify, send_from_directory; from flask_cors import CORS; import json; import os; app = Flask(__name__, static_folder='.', static_url_path=''); CORS(app); @app.route('/'); def index(): return send_from_directory('.', 'index.html'); @app.route('/api/status'); def status(): return jsonify({'battery': 65, 'temperature': 32.5}); if __name__ == '__main__': app.run(host='0.0.0.0', port=8080)